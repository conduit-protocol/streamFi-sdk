# Stellar secret key for signing transactions (starts with S)
# Generate with: node -e "console.log(require('@stellar/stellar-sdk').Keypair.random().secret())"
VITE_STELLAR_SECRET=S...

# DripFactory contract address on the network (starts with C)
# Find testnet address in protocol docs or README
VITE_FACTORY_ADDRESS=C...
